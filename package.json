{
  "name": "casevault-website",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node build/cloudflare-pages.mjs --serve",
    "build": "node build/cloudflare-pages.mjs",
    "start": "node build/cloudflare-pages.mjs --serve",
    "validate": "node build/validate-site.mjs",
    "test": "npm run build && npm run validate",
    "cloudflare:check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  },
  "type": "module"
}
